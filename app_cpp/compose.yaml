services:
  quantDatabase:
    image: mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: example
      MARIADB_DATABASE: 'quant'
    ports:
      - 3306:3306
#     volumes:
#       - quant-db:/var/lib/mysql
# volumes :
#   quant-db: